context('Test general document', ()=>{
   describe('CFM functionalities', ()=>{
       it('verify warning comes up when user changes languages', ()=>{
           //TODO Spec: browser warning (ala CODAP Leave Site warning) comes up when user changes languages
       })
   })
});